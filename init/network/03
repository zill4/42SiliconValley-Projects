# Q: Identify the MAC address of the Wi-Fi card
ifconfig en1 | grep ether | awk '/ether/{print $2}'
# 0: ifconfig en1, grabs en1 the current machine
# 1: grep ether, targeting the ether catagory
# 2: awk scans for the ether attribute and prints the second property